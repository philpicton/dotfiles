-- return {
--   { "mason-org/mason.nvim", version = "1.11.0" },
--   { "mason-org/mason-lspconfig.nvim", version = "1.32.0" },
-- }
-- I don't remember why i needed this workaround- it fixed something but now everything seems to work. I've disabled it for now 
-- TODO: delete this 
